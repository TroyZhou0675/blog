---
import Post from '../../layouts/Post.astro';
---

<Post
  title="MRI序列设计入门：从脉冲序列图读懂扫描原理"
  date="2026-02-10"
  category="序列设计"
  tags={['序列设计', '脉冲序列', '入门']}
  views={756}
  excerpt="读懂脉冲序列图是理解MRI序列的第一步。本文以SE序列和GRE序列为例，手把手教你从序列图中识别RF脉冲、梯度场和采集窗口。"
  cover="https://images.unsplash.com/photo-1628595351029-c2bf17511435?w=900&auto=format&fit=crop"
  activeNav="blog"
>

## 为什么需要学看脉冲序列图？

MRI 的本质是"控制电磁波与人体氢质子对话"的过程。脉冲序列图（Pulse Sequence Diagram）就是这个对话的乐谱——看懂它，你就能理解任意序列的设计意图，而不是死记硬背参数表。

## 序列图的三大基本元素

### 1. RF 脉冲（射频脉冲）

通常画在**最上方**，表示发射的射频能量。关键参数：
- **翻转角（Flip Angle）**：决定纵向磁化矢量被倾倒多少
- **带宽（Bandwidth）**：影响选择性和化学位移

### 2. 梯度场（Gradient）

分为三个方向，用不同颜色/线型表示：
- **Gslice**（层选梯度）：配合特定频率的 RF 脉冲选择成像层
- **Gphase**（相位编码梯度）：在相位方向上引入空间编码的相位差
- **Gread**（读出梯度/频率编码）：在频率编码方向采集信号

### 3. 信号采集（ADC / Acquisition）

通常画在序列图**最下方**，对应 k-space 数据的填充时刻。

## 两大基础序列：SE vs. GRE

### 自旋回波（Spin Echo, SE）

**原理**：用 90° RF 脉冲将质子群倾倒，经历短暂失相后，再用 180° 脉冲聚焦，消除主磁场不均匀带来的相位累积。

```
[90°] ─→ [180°] ─→ [回波] ─→ 采集
  │                   ↑
  └─ T2衰减 + 主磁场不均匀抵消
```

**特点**：
- 对主磁场不均匀不敏感（优于 GRE）
- T2 权重可通过 TE 调节
- 扫描时间相对较长

### 梯度回波（Gradient Echo, GRE）

**原理**：不使用 180° 重聚焦脉冲，仅靠读出梯度的反转实现相位重聚焦。

```
[α°] ─→ [-G] ─→ [回波] ─→ 采集
        ↑
    梯度反转代替180°聚焦
```

**特点**：
- 扫描速度快（可用于屏气扫描）
- 对磁场不均匀敏感
- 可做出非常小的翻转角（FLASH 序列）

## 如何读懂一个完整序列

拿到一张脉冲序列图，按这个顺序分析：

1. **先找 RF 脉冲**：判断是 90°/180° 组合（SE 类）还是小角度（GRE 类）
2. **再看层选梯度**：与 RF 带宽匹配，确定层厚和层间距
3. **然后相位编码梯度**：找到其位置和幅度——幅度决定相位编码方向的空间分辨率
4. **最后读出梯度**：与采集窗口（ADC）重叠，宽度影响带宽和读出方向分辨率
5. **数 TR 和 TE**：完整扫描一遍后估算 Repetition Time 和 Echo Time

## 从 SE 到临床常用序列

| 序列 | 基于 | 关键改动 |
|------|------|---------|
| T1WI SE | SE | 短 TR（≈ 500 ms），短 TE（≈ 10 ms）|
| T2WI FSE | SE + 多个 180° | 快速自旋回波，减少扫描时间 |
| FLAIR | T2WI FSE | 抑制自由水的 T2 高信号 |
| GRE (FLASH) | GRE | 小翻转角，极短 TR |

---

*理解序列图是进入 MRI 工程世界的钥匙，下一篇我们将深入 TSE/FSE 的加速技术。*

</Post>
