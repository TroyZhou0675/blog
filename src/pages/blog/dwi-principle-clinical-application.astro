---
import Post from '../../layouts/Post.astro';
---

<Post
  title="DWI扩散加权成像：原理与临床应用"
  date="2025-12-10"
  category="MRI"
  tags={['MRI', 'DWI', '扩散成像']}
  views={3201}
  excerpt="扩散加权成像（DWI）是检测急性脑梗死最敏感的MRI序列。本文系统介绍DWI的物理学原理、b值选择，以及在肿瘤分级中的应用。"
  cover="https://images.unsplash.com/photo-1530026405186-ed1f139313f8?w=900&auto=format&fit=crop"
  activeNav="blog"
>

## DWI 为什么重要

扩散加权成像（Diffusion Weighted Imaging, DWI）是 MRI 史上最重要的技术进步之一。它能在超急性期（发病 30 分钟内）检测到脑缺血改变，比常规 T2WI 敏感得多。

临床上，DWI 主要用于：
- **急性脑梗死的早期诊断**
- **脓肿 vs 肿瘤的鉴别**
- **肿瘤分级（恶变程度）**
- **神经退行性疾病的评估**

## 物理学原理

DWI 基于布朗运动（分子热运动）的原理。在均匀无限制的水中，水分子向各个方向随机运动（等向性扩散）。但在生物组织中，水分子的扩散受到细胞结构的限制：

- **细胞内水**（intracellular）：扩散受限
- **细胞外水**（extracellular）：相对自由
- **细胞膜/轴突**：对某些方向扩散有屏障

### b 值：扩散加权强度的标尺

DWI 用一个叫做 **b 值**的参数控制扩散加权程度：

```
b = γ² G² δ² (Δ - δ/3)
```

其中 γ 是旋磁比，G 是梯度幅度，δ 是梯度持续时间，Δ 是两个梯度之间的间隔。

**临床常用 b 值**：
- **b = 0**：接近常规 T2WI，无扩散加权
- **b = 1000 s/mm²**：最常用，急性脑梗死检测
- **b = 2000 s/mm²**：高 b 值，更敏感但 SNR 下降

> **b 值越大**，扩散加权越重，正常组织与病变对比度差异越大，但信噪比（SNR）也越低。

## DWI 如何成像

DWI 使用**单次激发自旋回波平面成像**（SS-EPI），是目前临床最快的扩散成像技术：

```python
# DWI 典型协议参数
params = &#123;
    "b_values": [0, 1000],      # 两个 b 值
    "directions": 3,            # 至少 3 个方向以检测等向性扩散
    "slice_thickness": 5,       # mm
    "gap": 1.5,                  # mm
    "TR": 3000,                  # ms
    "TE": 80,                    # ms（EPI 需要短 TE）
&#125;
```

### 等向性扩散图（IsoDWI）与 ADC 图

从多个扩散方向的原始数据可以计算两个衍生图像：

- **等向性扩散图（IsoDWI）**：所有方向扩散加权的均值，类似"融合图"
- **表观扩散系数图（ADC Map）**：去除了 T2 透射效应的"纯扩散图"，定量描述扩散速率

```
ADC = ln(S₂/S₁) / (b₁ - b₂)
```

在 ADC 图上：
- **急性梗死**：ADC **低**（扩散受限）→ 呈暗黑色
- **慢性梗死**：ADC **高**（囊性变/脑软化）→ 呈亮白色
- **脓肿**：ADC **低**（高细胞密度）
- **表皮样囊肿**：ADC **低**（高粘稠度）

## ADC 值的定量参考

| 组织 | ADC 值（×10⁻⁶ mm²/s）|
|------|---------------------|
| 正常脑灰质 | 800–1000 |
| 正常脑白质 | 700–900 |
| 急性梗死（&lt; 1 周）| 550–700 |
| 慢性梗死（&gt; 2 周）| 1200–1500 |
| 脓肿 | 600–900 |
| 高级别胶质瘤 | 900–1400 |
| 脑膜瘤 | 1000–1400 |

## 临床案例：急性脑梗死

患者突发右侧偏瘫 2 小时：

| 序列 | 表现 | 解读 |
|------|------|------|
| T2WI | 未见明确异常 | 早期 T2 尚未显示 |
| FLAIR | 脑回略肿胀 | 不特异 |
| **DWI** | 左侧半卵圆中心高信号 | **DWI 高信号 = 急性梗死** |
| ADC | 低信号 | 确认扩散受限 |

DWI 阳性 + ADC 阴性的组合是急性脑缺血的金标准。

## DWI 的局限

- **磁敏感伪影**：EPI 采集对骨-气界面附近（颅底）信号变形大
- **分辨率有限**：EPI 固有分辨率较低，脑干部位判读困难
- **运动敏感**：患者不自主运动（抖肩、血管搏动）会产生伪影

---

*DWI 是每一个 MRI 影像学习者必须掌握的核心序列，下一篇我们来看更高级的扩散模型——DTI（扩散张量成像）。*

</Post>
