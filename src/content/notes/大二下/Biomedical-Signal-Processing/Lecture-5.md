---
title: 'Lecture 5'
date: '2026-03-31'
category: '大二下 / 生物医学信号处理'
excerpt: |
  FIR系统的b系数就是h[n]
---

FIR系统的b系数就是$h[n]$

$$y[n] = 3x[n]+2x[n-3]+x[n-6]$$

这个表达式既是差分方程，又是卷积表达式的展开

常见窗函数：矩形窗、三角窗、Hanning窗（效果：柔美、模糊    |   本质：低通滤波器）

$h[n]$含负号：变化率、跳跃检测、边缘检测：

$$h[n] = \{1 -1\}$$

$$y[n] = x[n]-x[n-1]$$

常见含有负值的卷积核：

{1 -1} 一阶差分

{1 -2  1}二阶差分、突变点

{-1 2 -1}峰值加强、锐化滤波

{1  0 -1}保留低频、带阻

{1  0 -1}近似微分、带通

图卷积核

**特征提取：模式匹配或者相似度估计**
如何评价两组数据的相似程度：点乘

问题：相位移动导致结果变化？

解决方案：使用卷积（兼具滑动和点乘）

互相关函数的定义：

$$R_{xy}(t) = \int_{-\infty}^{\infty}x(\tau)y(\tau -t )d\tau = x(t)*y(-t)$$

## 傅里叶级数和傅里叶变换
基、正交基、自由度

**工程中的逼近思想：**

泰勒展开：幂函数可算性强，将其他形式的函数拆分成幂函数的组合（幂级数）

函数正交：

若$f(x),g(x)$在$[a,b]$上带权函数$\rho (x)$正交，则：

$$(f,g) = \int_a^b\rho (x)f(x)g(x)dx = 0$$

**正交系：三角级数**

1, cosx, sinx, cos2x, sin2x, ……，cosnx, sinnx……